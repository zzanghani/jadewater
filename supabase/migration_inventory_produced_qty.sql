-- ============================================================================
-- 재고관리 — 주방 품목 생산량 예측을 위한 컬럼 추가.
-- 주방은 "전일재고 + 오늘생산 − 오늘재고 = 오늘 사용량"으로 요일별 평균을
-- 내서 다음날 생산 제안량을 계산한다. 홀 소모품은 이 컬럼을 안 쓰고
-- (계속 null) 전일-금일 차감만으로 소진 예상일을 계산한다.
-- Supabase 프로젝트의 SQL Editor에서 그대로 실행하세요.
-- ============================================================================

alter table public.inventory_counts
  add column if not exists produced_quantity numeric;
