-- ============================================================================
-- HR 직원 삭제를, DB에서 완전히 지워버리는 방식에서 "퇴사 처리"(기록은
-- 남기고 재직 목록에서만 빠짐)로 바꾼다.
--
-- Supabase 프로젝트의 SQL Editor에서 그대로 실행하세요. 그린/블루 두
-- 프로젝트 모두 각각 실행해야 합니다. migration_hr_full_roster.sql을
-- 먼저 실행한 뒤에 실행해야 합니다.
-- ============================================================================

alter table public.employees add column if not exists resigned_at date;
